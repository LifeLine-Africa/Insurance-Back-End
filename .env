# LifeLine Africa Insurance API - Environment Configuration
# Copy this file to .env and fill in your actual values

# Application Configuration
DEBUG=false
HOST=0.0.0.0
PORT=5000
SECRET_KEY=your-super-secret-key-here-generate-with-python-secrets

# Force HTTPS in production
FORCE_HTTPS=true

# CORS Configuration (comma-separated list of allowed origins)
ALLOWED_ORIGINS=https://yourdomain.com,https://api.yourdomain.com

# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/insurance_db
# For MongoDB Atlas (cloud):
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/insurance_db?retryWrites=true&w=majority

# Email Configuration (SMTP)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=465
SMTP_USERNAME=J.chukwuony@alustudent.com
SMTP_PASSWORD=ljol rjet wgyg fgbe
EMAIL_FROM_NAME=LifeLine Africa Insurance
EMAIL_FROM_ADDRESS=noreply@lifelineafrica.com

# Email Recipients (JSON arrays)
PRIMARY_RECIPIENTS=["admin@lifelineafrica.com", "insurance@lifelineafrica.com"]
CC_RECIPIENTS=["notifications@lifelineafrica.com"]

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=logs/insurance_api.log

# Rate Limiting (requests per time period)
RATE_LIMIT_DEFAULT=200 per day, 50 per hour
RATE_LIMIT_SUBMIT=10 per minute
RATE_LIMIT_DOWNLOAD=5 per minute

# File Upload Limits (in bytes)
MAX_CONTENT_LENGTH=16777216  # 16MB

# Production Deployment Options
# GUNICORN_WORKERS=4
# GUNICORN_TIMEOUT=30
# GUNICORN_BIND=0.0.0.0:5000